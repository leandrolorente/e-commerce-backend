# Variáveis de Ambiente - Backend GuaranaTatto

# Banco de dados PostgreSQL
# Exemplo: postgresql://usuario:senha@localhost:5432/guarana_tattoo?schema=public
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# JWT Authentication
JWT_SECRET="seu-secret-super-seguro-de-pelo-menos-32-caracteres"
JWT_EXPIRES_IN="7d"

# CORS - Origens permitidas (separadas por vírgula)
# Produção: domínio do frontend
# Desenvolvimento: localhost do Angular
CORS_ORIGIN="http://localhost:4200,https://seu-frontend.vercel.app"

# Node Environment
NODE_ENV="development"

# Porta do servidor
PORT=3000

# ===== INSTRUÇÕES =====
# 
# 1. Copie este arquivo para .env:
#    cp .env.example .env
#
# 2. Preencha os valores reais (NUNCA commite o arquivo .env)
#
# 3. Para desenvolvimento local:
#    - DATABASE_URL: use PostgreSQL local
#    - CORS_ORIGIN: http://localhost:4200
#    - NODE_ENV: development
#
# 4. Para produção (Railway/Render):
#    - DATABASE_URL: fornecida automaticamente pela plataforma
#    - JWT_SECRET: gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#    - CORS_ORIGIN: URL do frontend em produção
#    - NODE_ENV: production
#
# 5. NUNCA compartilhe o arquivo .env ou commite ele no Git!
